export enum Route {
    Polygon,
    Hop
}

export enum Asset {
    Eth,
    Usdc
}

export enum ChainId {
    Mainnet,
    Polygon
}

export enum BridgeId {
    Polygon,
    Hop
}

export enum RouteId {
    Polygon,
    Hop
}