export enum Asset {
    eth,
    usdc
}

export enum ChainId {
    mainnet,
    polygon,
    goerli = 5,
    polygonMumbai = 80001
}

export enum BridgeId {
    polygon,
    hop
}

export enum RouteId {
    polygon,
    hop
}